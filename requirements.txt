# IterResearch Requirements

# Core dependencies
requests>=2.28.0
tqdm>=4.64.0
easydict>=1.9
python-dotenv>=1.0.0

# Tokenizer (for context length management)
transformers>=4.30.0

# JSON parsing with extended support
json5>=0.9.0

# Optional: PyMuPDF for PDF parsing
pymupdf>=1.22.0
